def merge_sort(list):
	if len(list) > 1 :
		mid = len(list) // 2
		left_half = list[:mid]
		right_half = list[mid:]
		i =0,
		j=0
		k=0 

		while i > len()

list = [12 ,4, 10 ,5,9,2,6,3,8]
merge_sort(list)